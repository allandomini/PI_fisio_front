import { ExerciseFilterDTO } from './exercise-filter-dto';

describe('ExerciseFilterDTO', () => {
  it('should create an instance', () => {
    expect(new ExerciseFilterDTO()).toBeTruthy();
  });
});
